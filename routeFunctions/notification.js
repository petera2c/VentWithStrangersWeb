const Notifications = require("../models/Notifications");
const Settings = require("../models/Settings");
const User = require("../models/User");

const saveNotification = (senderID, type, userID) => {};
module.exports = {};
